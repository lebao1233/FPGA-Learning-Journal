//Temporary: set the counter to 5
module shift_register_tb();
  reg clk;
  reg rst_n;
  wire [5:0] led;
  shift_register DUT(.clk(clk),
                     .rst_n(rst_n),
                     .led(led));
   always #10 clk=~clk;
initial begin
    clk = 0;
    rst_n = 0;

    $dumpfile("dump.vcd");
    $dumpvars(0, shift_register_tb);

    #30;
    rst_n = 1;

  
    #300;

    $finish;
end
endmodule
    
    